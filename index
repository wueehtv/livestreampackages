<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>WUEEH TV - Premium Livestreaming Solutions</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/lottie-web/5.9.4/lottie.min.js"></script>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        :root {
            --primary: #1a1a2e;
            --secondary: #16213e;
            --accent: #0f3460;
            --highlight: #e94560;
            --blue: #1e40af;
            --light-blue: #3b82f6;
            --gold: #f59e0b;
            --light: #f5f5f5;
            --gray: #8a8a8a;
            --dark-bg: #0a0a1a;
            --dark-card: #11112e;
        }
        
        [data-theme="dark"] {
            --primary: #0a0a1a;
            --secondary: #11112e;
            --accent: #1e3a5f;
            --light: #1a1a2e;
            --gray: #a0a0a0;
        }
        
        body {
            background: linear-gradient(135deg, var(--blue), var(--light-blue));
            color: #333;
            line-height: 1.6;
            padding: 20px;
            min-height: 100vh;
            overflow-x: hidden;
            transition: all 0.3s ease;
        }
        
        [data-theme="dark"] body {
            background: linear-gradient(135deg, var(--dark-bg), var(--primary));
            color: #f0f0f0;
        }
        
        .container {
            max-width: 1400px;
            margin: 0 auto;
        }
        
        .rate-card {
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.3);
            overflow: hidden;
            position: relative;
            transition: all 0.3s ease;
        }
        
        [data-theme="dark"] .rate-card {
            background: var(--dark-card);
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.6);
        }
        
        /* Theme Toggle */
        .theme-toggle {
            position: fixed;
            top: 20px;
            right: 20px;
            z-index: 1000;
            background: var(--highlight);
            width: 50px;
            height: 50px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
            transition: all 0.3s ease;
        }
        
        .theme-toggle:hover {
            transform: scale(1.1) rotate(15deg);
        }
        
        .theme-toggle i {
            color: white;
            font-size: 1.2rem;
        }
        
        /* Voice Control */
        .voice-control {
            position: fixed;
            top: 80px;
            right: 20px;
            z-index: 1000;
            background: var(--accent);
            width: 50px;
            height: 50px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
            transition: all 0.3s ease;
        }
        
        .voice-control.listening {
            background: var(--highlight);
            animation: pulse 1.5s infinite;
        }
        
        @keyframes pulse {
            0% { transform: scale(1); }
            50% { transform: scale(1.1); }
            100% { transform: scale(1); }
        }
        
        .voice-control i {
            color: white;
            font-size: 1.2rem;
        }
        
        .voice-feedback {
            position: fixed;
            top: 140px;
            right: 20px;
            background: rgba(0, 0, 0, 0.8);
            color: white;
            padding: 10px 15px;
            border-radius: 10px;
            font-size: 0.9rem;
            max-width: 200px;
            display: none;
            z-index: 1000;
        }
        
        .header {
            background: linear-gradient(rgba(26, 26, 46, 0.9), rgba(22, 33, 62, 0.9)), url('https://images.unsplash.com/photo-1533174072545-7a4b6ad7a6c3?ixlib=rb-4.0.3&auto=format&fit=crop&w=1920&q=80') center/cover;
            color: white;
            padding: 50px 30px;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        [data-theme="dark"] .header {
            background: linear-gradient(rgba(10, 10, 26, 0.9), rgba(17, 17, 46, 0.9)), url('https://images.unsplash.com/photo-1533174072545-7a4b6ad7a6c3?ixlib=rb-4.0.3&auto=format&fit=crop&w=1920&q=80') center/cover;
        }

        /* Floating elements animation */
        .floating-elements {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 0;
        }
        
        .floating-element {
            position: absolute;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            animation: float 15s infinite linear;
        }
        
        @keyframes float {
            0% {
                transform: translateY(0) rotate(0deg);
                opacity: 0;
            }
            10% {
                opacity: 1;
            }
            90% {
                opacity: 1;
            }
            100% {
                transform: translateY(-1000px) rotate(720deg);
                opacity: 0;
            }
        }

        .logo-container {
            margin-bottom: 20px;
            position: relative;
            z-index: 2;
        }

        .logo {
            width: 280px;
            height: auto;
            margin: 0 auto 20px;
            object-fit: contain;
            filter: drop-shadow(0 5px 15px rgba(0, 0, 0, 0.3));
            transition: transform 0.3s ease;
        }

        .logo:hover {
            transform: scale(1.05);
        }
        
        .tagline {
            font-size: 2.5rem;
            margin-bottom: 20px;
            font-weight: 800;
            letter-spacing: 1.5px;
            text-transform: uppercase;
            text-shadow: 0 2px 10px rgba(0, 0, 0, 0.3);
            background: linear-gradient(45deg, #fff, #f0f0f0);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            position: relative;
            z-index: 2;
        }
        
        .intro {
            max-width: 900px;
            margin: 0 auto 30px;
            font-size: 1.3rem;
            line-height: 1.8;
            font-weight: 300;
            text-shadow: 0 1px 3px rgba(0, 0, 0, 0.3);
            position: relative;
            z-index: 2;
        }
        
        .cta-button {
            display: inline-block;
            background: var(--highlight);
            color: white;
            padding: 18px 45px;
            border-radius: 50px;
            font-size: 1.3rem;
            font-weight: 700;
            text-decoration: none;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            transition: all 0.4s ease;
            box-shadow: 0 8px 25px rgba(233, 69, 96, 0.4);
            position: relative;
            overflow: hidden;
            z-index: 1;
        }
        
        .cta-button:before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
            transition: 0.6s;
            z-index: -1;
        }
        
        .cta-button:hover {
            background: #ff2e63;
            transform: translateY(-5px) scale(1.05);
            box-shadow: 0 15px 30px rgba(233, 69, 96, 0.6);
        }
        
        .cta-button:hover:before {
            left: 100%;
        }
        
        /* Live Stream Demo */
        .live-demo {
            padding: 70px 40px;
            background: var(--light);
            text-align: center;
        }
        
        [data-theme="dark"] .live-demo {
            background: var(--dark-bg);
        }
        
        .stream-container {
            max-width: 900px;
            margin: 0 auto;
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.3);
            position: relative;
        }
        
        .stream-placeholder {
            background: #000;
            height: 500px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.2rem;
        }
        
        .stream-controls {
            display: flex;
            justify-content: center;
            gap: 20px;
            margin-top: 30px;
        }
        
        .stream-btn {
            padding: 12px 25px;
            background: var(--accent);
            color: white;
            border: none;
            border-radius: 25px;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        
        .stream-btn:hover {
            background: var(--highlight);
            transform: translateY(-3px);
        }
        
        /* Package Customizer */
        .package-customizer {
            padding: 70px 40px;
            background: white;
        }
        
        [data-theme="dark"] .package-customizer {
            background: var(--dark-card);
        }
        
        .customizer-container {
            max-width: 1000px;
            margin: 0 auto;
        }
        
        .customizer-sidebar {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 30px;
            margin-bottom: 40px;
        }
        
        .feature-category {
            background: var(--light);
            padding: 25px;
            border-radius: 15px;
        }
        
        [data-theme="dark"] .feature-category {
            background: var(--dark-bg);
        }
        
        .feature-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 15px 0;
            border-bottom: 1px solid #eee;
        }
        
        [data-theme="dark"] .feature-item {
            border-bottom: 1px solid #333;
        }
        
        .feature-toggle {
            width: 50px;
            height: 25px;
            background: #ddd;
            border-radius: 25px;
            position: relative;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        
        .feature-toggle.active {
            background: var(--highlight);
        }
        
        .feature-toggle::after {
            content: '';
            position: absolute;
            width: 21px;
            height: 21px;
            background: white;
            border-radius: 50%;
            top: 2px;
            left: 2px;
            transition: all 0.3s ease;
        }
        
        .feature-toggle.active::after {
            left: 27px;
        }
        
        .customizer-preview {
            background: var(--light);
            padding: 30px;
            border-radius: 15px;
            text-align: center;
        }
        
        [data-theme="dark"] .customizer-preview {
            background: var(--dark-bg);
        }
        
        .total-price {
            font-size: 2.5rem;
            font-weight: 800;
            color: var(--highlight);
            margin: 20px 0;
        }
        
        /* 3D Studio Tour */
        .studio-tour {
            padding: 70px 40px;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            color: white;
            text-align: center;
        }
        
        .tour-container {
            max-width: 800px;
            height: 500px;
            margin: 0 auto;
            background: #000;
            border-radius: 15px;
            overflow: hidden;
            position: relative;
        }
        
        #three-container {
            width: 100%;
            height: 100%;
        }
        
        .tour-controls {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin-top: 20px;
        }
        
        /* Packages Section */
        .packages {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(380px, 1fr));
            gap: 35px;
            padding: 60px 40px;
            background: #f8f9fa;
        }
        
        [data-theme="dark"] .packages {
            background: var(--dark-bg);
        }
        
        .package-card {
            background: white;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.12);
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            position: relative;
            border: 1px solid #eaeaea;
        }
        
        [data-theme="dark"] .package-card {
            background: var(--dark-card);
            border: 1px solid #333;
        }
        
        .package-card:hover {
            transform: translateY(-20px);
            box-shadow: 0 25px 60px rgba(0, 0, 0, 0.18);
        }
        
        .package-card.featured {
            border: 4px solid var(--highlight);
            transform: scale(1.05);
            box-shadow: 0 20px 50px rgba(233, 69, 96, 0.3);
        }
        
        .package-card.featured:hover {
            transform: scale(1.05) translateY(-20px);
            box-shadow: 0 30px 70px rgba(233, 69, 96, 0.4);
        }
        
        .featured-badge {
            position: absolute;
            top: 25px;
            right: -35px;
            background: linear-gradient(45deg, var(--highlight), #ff2e63);
            color: white;
            padding: 12px 50px;
            transform: rotate(45deg);
            font-size: 0.9rem;
            font-weight: bold;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
            z-index: 10;
            letter-spacing: 1px;
        }
        
        .package-image {
            height: 220px;
            background-size: cover;
            background-position: center;
            position: relative;
            transition: transform 0.4s ease;
        }

        .package-card:hover .package-image {
            transform: scale(1.05);
        }
        
        .package-1 .package-image {
            background-image: url('https://images.unsplash.com/photo-1563291074-2bf8677ac0e5?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1412&q=80');
        }
        
        .package-2 .package-image {
            background-image: url('https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1374&q=80');
        }
        
        .package-3 .package-image {
            background-image: url('https://images.unsplash.com/photo-1516035069371-29a1b244cc32?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=1528&q=80');
        }
        
        .package-overlay {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            background: linear-gradient(transparent, rgba(0, 0, 0, 0.8));
            color: white;
            padding: 25px;
        }
        
        .package-name {
            font-size: 1.9rem;
            margin-bottom: 8px;
            font-weight: 800;
        }
        
        .package-price {
            font-size: 2.8rem;
            font-weight: 900;
            margin-bottom: 5px;
            color: var(--gold);
            text-shadow: 2px 2px 5px rgba(0, 0, 0, 0.5);
        }
        
        .package-duration {
            font-size: 1.1rem;
            opacity: 0.9;
            font-weight: 500;
        }
        
        .package-details {
            padding: 35px;
        }
        
        [data-theme="dark"] .package-details {
            color: #f0f0f0;
        }
        
        .package-details ul {
            list-style-type: none;
            margin-bottom: 35px;
        }
        
        .package-details li {
            padding: 14px 0;
            border-bottom: 1px solid #f0f0f0;
            position: relative;
            padding-left: 45px;
            font-size: 1.1rem;
            transition: all 0.3s ease;
        }

        [data-theme="dark"] .package-details li {
            border-bottom: 1px solid #333;
        }

        .package-details li:hover {
            background: #f8f9fa;
            transform: translateX(5px);
        }
        
        [data-theme="dark"] .package-details li:hover {
            background: var(--dark-bg);
        }
        
        .package-details li:before {
            content: "✓";
            position: absolute;
            left: 0;
            color: var(--highlight);
            font-weight: bold;
            font-size: 1.4rem;
            transition: transform 0.3s ease;
        }

        .package-details li:hover:before {
            transform: scale(1.3);
        }
        
        .ideal-for {
            background: linear-gradient(135deg, #f8f9fa, #e9ecef);
            padding: 25px;
            border-radius: 12px;
            font-style: italic;
            margin-top: 25px;
            border-left: 6px solid var(--accent);
            position: relative;
            transition: all 0.3s ease;
        }

        [data-theme="dark"] .ideal-for {
            background: linear-gradient(135deg, var(--dark-bg), var(--primary));
        }

        .ideal-for:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
        }
        
        .ideal-for:before {
            content: "★";
            position: absolute;
            top: 20px;
            left: -20px;
            background: var(--accent);
            color: white;
            width: 35px;
            height: 35px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1rem;
            box-shadow: 0 3px 10px rgba(0, 0, 0, 0.2);
        }
        
        .ideal-for strong {
            color: var(--accent);
            font-size: 1.2rem;
            display: block;
            margin-bottom: 8px;
        }
        
        /* Portfolio Section */
        .portfolio-section {
            padding: 70px 40px;
            background: linear-gradient(135deg, #f8f9fa, #e9ecef);
        }
        
        [data-theme="dark"] .portfolio-section {
            background: linear-gradient(135deg, var(--dark-bg), var(--primary));
        }
        
        .portfolio-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            margin-top: 50px;
        }
        
        .portfolio-item {
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
            transition: all 0.4s ease;
            position: relative;
            height: 250px;
            cursor: pointer;
        }
        
        .portfolio-item:hover {
            transform: translateY(-10px);
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.2);
        }
        
        .portfolio-image {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.5s ease;
        }
        
        .portfolio-item:hover .portfolio-image {
            transform: scale(1.1);
        }
        
        .portfolio-overlay {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            background: linear-gradient(transparent, rgba(0, 0, 0, 0.8));
            color: white;
            padding: 25px;
            transform: translateY(100px);
            transition: transform 0.4s ease;
        }
        
        .portfolio-item:hover .portfolio-overlay {
            transform: translateY(0);
        }
        
        .portfolio-title {
            font-size: 1.4rem;
            font-weight: 700;
            margin-bottom: 8px;
        }
        
        .portfolio-category {
            font-size: 1rem;
            opacity: 0.9;
        }
        
        /* Lightbox */
        .lightbox {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.9);
            display: none;
            align-items: center;
            justify-content: center;
            z-index: 2000;
        }
        
        .lightbox-content {
            max-width: 90%;
            max-height: 90%;
            position: relative;
        }
        
        .lightbox-close {
            position: absolute;
            top: 20px;
            right: 20px;
            color: white;
            font-size: 2rem;
            cursor: pointer;
        }
        
        /* Booking System */
        .booking-system {
            padding: 70px 40px;
            background: white;
        }
        
        [data-theme="dark"] .booking-system {
            background: var(--dark-card);
        }
        
        .booking-container {
            max-width: 1000px;
            margin: 0 auto;
        }
        
        .calendar-view {
            background: var(--light);
            padding: 30px;
            border-radius: 15px;
            margin-bottom: 30px;
        }
        
        [data-theme="dark"] .calendar-view {
            background: var(--dark-bg);
        }
        
        .calendar-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }
        
        .calendar-grid {
            display: grid;
            grid-template-columns: repeat(7, 1fr);
            gap: 10px;
        }
        
        .calendar-day {
            padding: 15px;
            text-align: center;
            border-radius: 10px;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        
        .calendar-day.available {
            background: #e8f5e8;
            color: #2e7d32;
        }
        
        .calendar-day.booked {
            background: #ffebee;
            color: #c62828;
            cursor: not-allowed;
        }
        
        .calendar-day:hover:not(.booked) {
            background: var(--accent);
            color: white;
        }
        
        .booking-form {
            background: var(--light);
            padding: 30px;
            border-radius: 15px;
        }
        
        [data-theme="dark"] .booking-form {
            background: var(--dark-bg);
        }
        
        .form-group {
            margin-bottom: 20px;
        }
        
        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
        }
        
        .form-group input,
        .form-group select,
        .form-group textarea {
            width: 100%;
            padding: 12px 15px;
            border: 1px solid #ddd;
            border-radius: 8px;
            font-size: 1rem;
        }
        
        [data-theme="dark"] .form-group input,
        [data-theme="dark"] .form-group select,
        [data-theme="dark"] .form-group textarea {
            background: var(--dark-card);
            border: 1px solid #333;
            color: white;
        }
        
        /* Enhanced Crew Section */
        .crew-section {
            padding: 70px 40px;
            background: var(--light);
        }
        
        [data-theme="dark"] .crew-section {
            background: var(--dark-bg);
        }
        
        .crew-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            margin-top: 50px;
        }
        
        .crew-item {
            background: white;
            border-radius: 15px;
            padding: 30px;
            text-align: center;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
        }
        
        [data-theme="dark"] .crew-item {
            background: var(--dark-card);
        }
        
        .crew-item:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
        }
        
        .crew-icon {
            width: 80px;
            height: 80px;
            background: var(--accent);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 20px;
            color: white;
            font-size: 1.8rem;
        }
        
        .crew-name {
            font-size: 1.4rem;
            font-weight: 700;
            margin-bottom: 15px;
            color: var(--accent);
        }
        
        .crew-description {
            font-size: 1rem;
            line-height: 1.6;
            color: var(--gray);
        }
        
        /* Enhanced Testimonials */
        .testimonials {
            padding: 70px 40px;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            color: white;
        }
        
        .testimonial-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            margin-top: 50px;
        }
        
        .testimonial {
            background: rgba(255, 255, 255, 0.1);
            padding: 30px;
            border-radius: 15px;
            backdrop-filter: blur(10px);
            transition: all 0.3s ease;
        }
        
        .testimonial:hover {
            transform: translateY(-10px);
            background: rgba(255, 255, 255, 0.15);
        }
        
        .testimonial-text {
            font-style: italic;
            margin-bottom: 20px;
            line-height: 1.6;
        }
        
        .testimonial-author {
            display: flex;
            align-items: center;
            gap: 15px;
        }
        
        .author-avatar {
            width: 50px;
            height: 50px;
            background: var(--gold);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            font-size: 1.2rem;
        }
        
        .author-info h4 {
            margin-bottom: 5px;
        }
        
        .author-info p {
            opacity: 0.8;
            font-size: 0.9rem;
        }
        
        /* Enhanced Terms Section */
        .terms {
            padding: 70px 40px;
            background: var(--light);
        }
        
        [data-theme="dark"] .terms {
            background: var(--dark-bg);
        }
        
        .terms-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            margin-top: 50px;
        }
        
        .term-item {
            background: white;
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
        }
        
        [data-theme="dark"] .term-item {
            background: var(--dark-card);
        }
        
        .term-item:hover {
            transform: translateY(-10px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
        }
        
        .term-item h3 {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 15px;
            color: var(--accent);
        }
        
        .term-item h3 i {
            font-size: 1.5rem;
        }
        
        /* Enhanced Footer */
        .footer {
            padding: 70px 40px;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            color: white;
            text-align: center;
        }
        
        .footer h3 {
            font-size: 2.2rem;
            margin-bottom: 20px;
        }
        
        .footer p {
            max-width: 600px;
            margin: 0 auto 40px;
            font-size: 1.2rem;
        }
        
        .contact-info {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            gap: 30px;
            margin-bottom: 40px;
        }
        
        .contact-item {
            display: flex;
            align-items: center;
            gap: 10px;
            font-size: 1.1rem;
        }
        
        .contact-item i {
            font-size: 1.5rem;
            color: var(--gold);
        }
        
        .social-links {
            display: flex;
            justify-content: center;
            gap: 20px;
        }
        
        .social-link {
            width: 50px;
            height: 50px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.2rem;
            transition: all 0.3s ease;
        }
        
        .social-link:hover {
            background: var(--highlight);
            transform: translateY(-5px);
        }
        
        /* AI Chatbot */
        .chat-bubble {
            position: fixed;
            bottom: 30px;
            right: 30px;
            width: 70px;
            height: 70px;
            background: var(--highlight);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 1.8rem;
            box-shadow: 0 10px 25px rgba(233, 69, 96, 0.4);
            cursor: pointer;
            z-index: 1000;
            transition: all 0.3s ease;
        }
        
        .chat-bubble:hover {
            transform: scale(1.1);
            box-shadow: 0 15px 30px rgba(233, 69, 96, 0.6);
        }
        
        .chat-window {
            position: fixed;
            bottom: 120px;
            right: 30px;
            width: 350px;
            height: 450px;
            background: white;
            border-radius: 20px;
            box-shadow: 0 15px 50px rgba(0, 0, 0, 0.3);
            z-index: 1000;
            display: none;
            flex-direction: column;
            overflow: hidden;
            transition: all 0.3s ease;
        }
        
        [data-theme="dark"] .chat-window {
            background: var(--dark-card);
        }
        
        .chat-header {
            background: linear-gradient(135deg, var(--accent), var(--secondary));
            color: white;
            padding: 20px;
            display: flex;
            align-items: center;
            gap: 15px;
        }
        
        .chat-avatar {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            background: var(--gold);
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            font-size: 1.2rem;
        }
        
        .chat-info h4 {
            margin-bottom: 5px;
            font-size: 1.2rem;
        }
        
        .chat-info p {
            opacity: 0.8;
            font-size: 0.9rem;
        }
        
        .chat-close {
            margin-left: auto;
            cursor: pointer;
            font-size: 1.2rem;
            opacity: 0.8;
            transition: opacity 0.3s ease;
        }
        
        .chat-close:hover {
            opacity: 1;
        }
        
        .chat-body {
            flex: 1;
            padding: 20px;
            overflow-y: auto;
            display: flex;
            flex-direction: column;
            gap: 15px;
        }
        
        .chat-message {
            max-width: 80%;
            padding: 12px 18px;
            border-radius: 18px;
            font-size: 0.95rem;
            line-height: 1.4;
        }
        
        .bot-message {
            background: #f0f2f5;
            align-self: flex-start;
            border-bottom-left-radius: 5px;
        }
        
        [data-theme="dark"] .bot-message {
            background: var(--dark-bg);
            color: white;
        }
        
        .user-message {
            background: var(--accent);
            color: white;
            align-self: flex-end;
            border-bottom-right-radius: 5px;
        }
        
        .chat-input {
            padding: 20px;
            border-top: 1px solid #eaeaea;
            display: flex;
            gap: 10px;
        }
        
        [data-theme="dark"] .chat-input {
            border-top: 1px solid #333;
        }
        
        .chat-input input {
            flex: 1;
            padding: 12px 15px;
            border: 1px solid #eaeaea;
            border-radius: 25px;
            outline: none;
            font-size: 0.95rem;
        }
        
        [data-theme="dark"] .chat-input input {
            background: var(--dark-bg);
            border: 1px solid #333;
            color: white;
        }
        
        .chat-send {
            width: 45px;
            height: 45px;
            border-radius: 50%;
            background: var(--accent);
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        
        .chat-send:hover {
            background: var(--highlight);
            transform: scale(1.05);
        }
        
        /* Language Switcher */
        .language-switcher {
            position: fixed;
            top: 20px;
            left: 20px;
            z-index: 1000;
        }
        
        .lang-btn {
            background: var(--accent);
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 25px;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 8px;
            transition: all 0.3s ease;
        }
        
        .lang-btn:hover {
            background: var(--highlight);
            transform: translateY(-2px);
        }
        
        .lang-dropdown {
            position: absolute;
            top: 100%;
            left: 0;
            background: white;
            border-radius: 10px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            display: none;
            overflow: hidden;
        }
        
        [data-theme="dark"] .lang-dropdown {
            background: var(--dark-card);
        }
        
        .lang-option {
            padding: 12px 20px;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        
        .lang-option:hover {
            background: var(--light);
        }
        
        [data-theme="dark"] .lang-option:hover {
            background: var(--dark-bg);
        }
        
        /* Progress Bar */
        .progress-bar {
            position: fixed;
            top: 0;
            left: 0;
            width: 0%;
            height: 4px;
            background: var(--highlight);
            z-index: 10000;
            transition: width 0.3s ease;
        }
        
        /* Enhanced Animations */
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(40px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        .animate {
            animation: fadeInUp 0.8s ease forwards;
        }
        
        .delay-1 {
            animation-delay: 0.2s;
        }
        
        .delay-2 {
            animation-delay: 0.4s;
        }
        
        .delay-3 {
            animation-delay: 0.6s;
        }

        /* Mobile Navigation */
        .mobile-nav {
            display: none;
            position: fixed;
            bottom: 20px;
            left: 50%;
            transform: translateX(-50%);
            background: var(--highlight);
            padding: 15px 25px;
            border-radius: 50px;
            z-index: 1000;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
        }

        .mobile-nav a {
            color: white;
            text-decoration: none;
            font-weight: 600;
            font-size: 1.1rem;
        }
        
        /* Responsive Design */
        @media (max-width: 1200px) {
            .packages {
                grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
                gap: 30px;
                padding: 50px 30px;
            }
            
            .package-card.featured {
                transform: none;
            }
            
            .package-card.featured:hover {
                transform: translateY(-20px);
            }
        }
        
        @media (max-width: 768px) {
            body {
                padding: 15px;
            }
            
            .header {
                padding: 40px 20px;
            }
            
            .logo {
                width: 220px;
            }
            
            .tagline {
                font-size: 1.8rem;
            }
            
            .intro {
                font-size: 1.1rem;
            }
            
            .cta-button {
                padding: 15px 35px;
                font-size: 1.1rem;
            }
            
            .packages {
                grid-template-columns: 1fr;
                padding: 40px 20px;
                gap: 25px;
            }
            
            .package-card.featured {
                transform: none;
            }
            
            .package-card.featured:hover {
                transform: translateY(-15px);
            }
            
            .featured-badge {
                right: -30px;
                padding: 10px 40px;
                font-size: 0.8rem;
            }
            
            .live-demo, .package-customizer, .studio-tour, .portfolio-section, .booking-system, .crew-section, .testimonials, .terms, .footer {
                padding: 50px 20px;
            }
            
            .section-title {
                font-size: 2rem;
            }
            
            .portfolio-grid {
                grid-template-columns: 1fr;
                gap: 20px;
            }
            
            .mobile-nav {
                display: block;
            }
            
            .chat-window {
                width: 300px;
                height: 400px;
                right: 20px;
                bottom: 100px;
            }
            
            .chat-bubble {
                right: 20px;
                bottom: 20px;
            }

            .customizer-sidebar {
                grid-template-columns: 1fr;
            }

            .theme-toggle, .voice-control {
                width: 45px;
                height: 45px;
            }
            
            .voice-feedback {
                right: 10px;
                max-width: 150px;
            }

            /* Adjust font sizes for mobile */
            .package-name {
                font-size: 1.6rem;
            }
            
            .package-price {
                font-size: 2.2rem;
            }
            
            .package-details {
                padding: 25px;
            }
            
            .package-details li {
                font-size: 1rem;
                padding: 12px 0;
                padding-left: 35px;
            }
            
            .crew-grid, .testimonial-grid, .terms-grid {
                grid-template-columns: 1fr;
            }
            
            .contact-info {
                flex-direction: column;
                gap: 20px;
            }
        }
        
        @media (max-width: 480px) {
            .header {
                padding: 30px 15px;
            }
            
            .logo {
                width: 180px;
            }
            
            .tagline {
                font-size: 1.5rem;
            }
            
            .intro {
                font-size: 1rem;
            }
            
            .cta-button {
                padding: 12px 30px;
                font-size: 1rem;
            }
            
            .package-details {
                padding: 20px;
            }
            
            .section-title {
                font-size: 1.7rem;
            }
            
            .package-name {
                font-size: 1.4rem;
            }
            
            .package-price {
                font-size: 2rem;
            }
            
            .chat-window {
                width: 280px;
                right: 10px;
            }
            
            .footer h3 {
                font-size: 1.8rem;
            }
            
            .footer p {
                font-size: 1rem;
            }
        }
    </style>
</head>
<body>
    <!-- Progress Bar -->
    <div class="progress-bar" id="progressBar"></div>

    <!-- Theme Toggle -->
    <div class="theme-toggle" id="themeToggle">
        <i class="fas fa-moon"></i>
    </div>

    <!-- Voice Control -->
    <div class="voice-control" id="voiceControl">
        <i class="fas fa-microphone"></i>
    </div>
    <div class="voice-feedback" id="voiceFeedback"></div>

    <!-- Language Switcher -->
    <div class="language-switcher">
        <button class="lang-btn" id="langBtn">
            <i class="fas fa-globe"></i>
            <span>EN</span>
            <i class="fas fa-chevron-down"></i>
        </button>
        <div class="lang-dropdown" id="langDropdown">
            <div class="lang-option" data-lang="en">English</div>
            <div class="lang-option" data-lang="sw">Swahili</div>
            <div class="lang-option" data-lang="fr">French</div>
        </div>
    </div>

    <div class="container">
        <div class="rate-card">
            <div class="header">
                <!-- Floating Elements -->
                <div class="floating-elements" id="floatingElements"></div>
                
                <!-- Logo Section -->
                <div class="logo-container">
                    <img src="https://i.ibb.co/SwYdxHkT/jpeg-removebg-preview.png" alt="WUEEH TV Logo" class="logo">
                </div>
                
                <div class="tagline animate">Premium Livestreaming & Event Production</div>
                <div class="intro animate delay-1">
                    Transform your events into unforgettable digital experiences with our professional multi-camera broadcast solutions. 
                    From intimate gatherings to large-scale conferences, we deliver cinematic quality streaming that captivates audiences worldwide.
                </div>
                
                <a href="#booking" class="cta-button animate delay-2">Book Your Event Now</a>
            </div>

            <!-- Live Stream Demo Section -->
            <div class="live-demo" id="liveDemo">
                <h2 class="section-title animate">Live Stream Demo</h2>
                <div class="stream-container animate delay-1">
                    <div class="stream-placeholder">
                        <iframe width="100%" height="100%" src="https://www.youtube.com/embed/eykGN_ggXGM?si=8TGKpXUaYM_YZbzk" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
                    </div>
                </div>
                <div class="stream-controls">
                    <button class="stream-btn" id="playDemo">
                        <i class="fas fa-play"></i> Play Demo
                    </button>
                    <button class="stream-btn" id="fullscreen">
                        <i class="fas fa-expand"></i> Fullscreen
                    </button>
                    <button class="stream-btn" id="vrMode">
                        <i class="fas fa-vr-cardboard"></i> VR Mode
                    </button>
                </div>
            </div>

            <!-- Package Customizer -->
            <div class="package-customizer" id="customizer">
                <h2 class="section-title animate">Build Your Custom Package</h2>
                <div class="customizer-container">
                    <div class="customizer-sidebar">
                        <div class="feature-category">
                            <h3>Camera Setup</h3>
                            <div class="feature-item">
                                <span>Single Camera</span>
                                <div class="feature-toggle" data-price="0"></div>
                            </div>
                            <div class="feature-item">
                                <span>Dual Camera</span>
                                <div class="feature-toggle" data-price="20000"></div>
                            </div>
                            <div class="feature-item">
                                <span>Three Camera</span>
                                <div class="feature-toggle" data-price="40000"></div>
                            </div>
                        </div>
                        <div class="feature-category">
                            <h3>Streaming Platforms</h3>
                            <div class="feature-item">
                                <span>Facebook Only</span>
                                <div class="feature-toggle" data-price="0"></div>
                            </div>
                            <div class="feature-item">
                                <span>Facebook + YouTube</span>
                                <div class="feature-toggle active" data-price="5000"></div>
                            </div>
                            <div class="feature-item">
                                <span>Multi-Platform</span>
                                <div class="feature-toggle" data-price="15000"></div>
                            </div>
                        </div>
                        <div class="feature-category">
                            <h3>Graphics Package</h3>
                            <div class="feature-item">
                                <span>Basic Graphics</span>
                                <div class="feature-toggle" data-price="0"></div>
                            </div>
                            <div class="feature-item">
                                <span>Enhanced Graphics</span>
                                <div class="feature-toggle active" data-price="10000"></div>
                            </div>
                            <div class="feature-item">
                                <span>Premium Graphics</span>
                                <div class="feature-toggle" data-price="25000"></div>
                            </div>
                        </div>
                        <div class="feature-category">
                            <h3>Additional Features</h3>
                            <div class="feature-item">
                                <span>Live Comment Moderation</span>
                                <div class="feature-toggle" data-price="8000"></div>
                            </div>
                            <div class="feature-item">
                                <span>Interactive Polls & Q&A</span>
                                <div class="feature-toggle" data-price="12000"></div>
                            </div>
                            <div class="feature-item">
                                <span>Highlight Reel</span>
                                <div class="feature-toggle active" data-price="15000"></div>
                            </div>
                        </div>
                    </div>
                    <div class="customizer-preview">
                        <h3>Your Custom Package</h3>
                        <div class="total-price">KES 70,000</div>
                        <button class="cta-button" id="savePackage">Save This Package</button>
                    </div>
                </div>
            </div>

            <!-- 3D Studio Tour -->
            <div class="studio-tour">
                <h2 class="section-title animate">Virtual Studio Tour</h2>
                <div class="tour-container">
                    <iframe width="100%" height="100%" src="https://www.youtube.com/embed/7YFlEThssaU" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
                </div>
                <div class="tour-controls">
                    <button class="stream-btn" id="rotateLeft">
                        <i class="fas fa-undo"></i> Rotate Left
                    </button>
                    <button class="stream-btn" id="rotateRight">
                        <i class="fas fa-redo"></i> Rotate Right
                    </button>
                    <button class="stream-btn" id="zoomIn">
                        <i class="fas fa-search-plus"></i> Zoom In
                    </button>
                    <button class="stream-btn" id="zoomOut">
                        <i class="fas fa-search-minus"></i> Zoom Out
                    </button>
                </div>
            </div>
            
            <!-- Packages Section -->
            <div class="packages">
                <div class="package-card package-1 animate">
                    <div class="package-image">
                        <div class="package-overlay">
                            <div class="package-name">ESSENTIAL STREAM</div>
                            <div class="package-price">KES 30,000</div>
                            <div class="package-duration">Per Event Day (8-10 hours)</div>
                        </div>
                    </div>
                    <div class="package-details">
                        <ul>
                            <li><strong>Single Camera Setup</strong> - Professional camera with fluid head tripod</li>
                            <li><strong>Basic Live Graphics</strong> - Custom lower thirds and branded opener</li>
                            <li><strong>Dual Platform Streaming</strong> - Facebook & YouTube simultaneously</li>
                            <li><strong>Program Recording</strong> - High-quality master recording</li>
                            <li><strong>Social Media Clip</strong> - One vertical clip optimized for social platforms</li>
                            <li><strong>Minimal Crew</strong> - Single operator + production assistant if needed</li>
                            <li><strong>Audio Support</strong> - Basic audio mixing with lavalier or handheld mic</li>
                        </ul>
                        <div class="ideal-for">
                            <strong>Perfect For:</strong> Small church services, classroom events, short talks, and intimate gatherings where simplicity meets professionalism.
                        </div>
                    </div>
                </div>
                
                <div class="package-card package-2 animate delay-1">
                    <div class="package-image">
                        <div class="package-overlay">
                            <div class="package-name">PROFESSIONAL BROADCAST</div>
                            <div class="package-price">KES 70,000</div>
                            <div class="package-duration">Per Event Day (8-10 hours)</div>
                        </div>
                    </div>
                    <div class="package-details">
                        <ul>
                            <li><strong>Dual Camera Setup</strong> - Two professional cameras with seamless switching</li>
                            <li><strong>Enhanced Graphics Package</strong> - Animated opener, lower thirds, title slates</li>
                            <li><strong>Multi-Platform Streaming</strong> - Facebook, YouTube + custom RTMP destination</li>
                            <li><strong>Advanced Audio</strong> - Multi-channel mixing with wireless lavaliers</li>
                            <li><strong>Social Interaction</strong> - Live comment moderation and display</li>
                            <li><strong>Vox-Pop Package</strong> - On-site interviews with attendees</li>
                            <li><strong>Dedicated Crew</strong> - Producer, TD, 2 camera ops, sound tech, stream engineer</li>
                            <li><strong>Deliverables</strong> - Full program master + 1 highlight reel + 2 social cuts</li>
                        </ul>
                        <div class="ideal-for">
                            <strong>Perfect For:</strong> Corporate presentations, panel discussions, small performances, and events requiring polished production values with audience engagement.
                        </div>
                    </div>
                </div>
                
                <div class="package-card package-3 featured animate delay-2">
                    <div class="featured-badge">MOST POPULAR</div>
                    <div class="package-image">
                        <div class="package-overlay">
                            <div class="package-name">ELITE PRODUCTION</div>
                            <div class="package-price">KES 100,000</div>
                            <div class="package-duration">Per Event Day (8-10 hours)</div>
                        </div>
                    </div>
                    <div class="package-details">
                        <ul>
                            <li><strong>Three Camera Setup</strong> - Full cinematic coverage with professional lenses</li>
                            <li><strong>Premium Graphics Package</strong> - Custom animated branding throughout</li>
                            <li><strong>Multi-Platform Mastery</strong> - Stream everywhere with redundant backup</li>
                            <li><strong>Broadcast Audio</strong> - Professional mixing with multiple wireless systems</li>
                            <li><strong>Interactive Experience</strong> - Live polls, Q&A, and moderated social display</li>
                            <li><strong>Interview Suite</strong> - Staged interviews with 2-camera coverage</li>
                            <li><strong>Full Production Crew</strong> - All roles covered for seamless execution</li>
                            <li><strong>Comprehensive Deliverables</strong> - Full event master, 2 highlight edits, 4 vertical social cuts</li>
                            <li><strong>ISO Recordings</strong> - Individual camera feeds for maximum editing flexibility</li>
                        </ul>
                        <div class="ideal-for">
                            <strong>Perfect For:</strong> Conferences, concerts, university events, product launches, and high-profile occasions where production excellence is non-negotiable.
                        </div>
                    </div>
                </div>
            </div>
            
            <!-- Portfolio Section -->
            <div class="portfolio-section">
                <h2 class="section-title animate">Our Recent Work</h2>
                <div class="portfolio-grid">
                    <div class="portfolio-item animate">
                        <img src="https://images.unsplash.com/photo-1540575467063-178a50c2df87?ixlib=rb-4.0.3&auto=format&fit=crop&w=1350&q=80" alt="Corporate Event" class="portfolio-image">
                        <div class="portfolio-overlay">
                            <div class="portfolio-title">TechConnect Africa Conference</div>
                            <div class="portfolio-category">Corporate Event</div>
                        </div>
                    </div>
                    <div class="portfolio-item animate delay-1">
                        <img src="https://images.unsplash.com/photo-1516450360452-9312f5e86fc7?ixlib=rb-4.0.3&auto=format&fit=crop&w=1350&q=80" alt="Music Concert" class="portfolio-image">
                        <div class="portfolio-overlay">
                            <div class="portfolio-title">Nairobi Jazz Festival</div>
                            <div class="portfolio-category">Music Concert</div>
                        </div>
                    </div>
                    <div class="portfolio-item animate delay-2">
                        <img src="https://images.unsplash.com/photo-1534438327276-14e5300c3a48?ixlib=rb-4.0.3&auto=format&fit=crop&w=1350&q=80" alt="University Event" class="portfolio-image">
                        <div class="portfolio-overlay">
                            <div class="portfolio-title">University Graduation</div>
                            <div class="portfolio-category">Academic Ceremony</div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Lightbox -->
            <div class="lightbox" id="lightbox">
                <div class="lightbox-content">
                    <img src="" alt="" id="lightboxImage">
                    <div class="lightbox-close" id="lightboxClose">
                        <i class="fas fa-times"></i>
                    </div>
                </div>
            </div>

            <!-- Booking System -->
            <div class="booking-system" id="booking">
                <h2 class="section-title animate">Book Your Event</h2>
                <div class="booking-container">
                    <div class="calendar-view animate">
                        <div class="calendar-header">
                            <h3>Select Your Event Date</h3>
                            <div>
                                <button id="prevMonth"><i class="fas fa-chevron-left"></i></button>
                                <span id="currentMonth">November 2024</span>
                                <button id="nextMonth"><i class="fas fa-chevron-right"></i></button>
                            </div>
                        </div>
                        <div class="calendar-grid" id="calendarGrid">
                            <!-- Calendar days will be generated here -->
                        </div>
                    </div>
                    <!-- Formspree Form -->
                    <form class="booking-form animate delay-1" action="https://formspree.io/f/mrbvzvvo" method="POST">
                        <h3>Event Details</h3>
                        <div class="form-group">
                            <label for="eventName">Event Name</label>
                            <input type="text" id="eventName" name="eventName" placeholder="Enter event name" required>
                        </div>
                        <div class="form-group">
                            <label for="clientName">Your Name</label>
                            <input type="text" id="clientName" name="clientName" placeholder="Enter your full name" required>
                        </div>
                        <div class="form-group">
                            <label for="email">Email Address</label>
                            <input type="email" id="email" name="email" placeholder="Enter your email" required>
                        </div>
                        <div class="form-group">
                            <label for="phone">Phone Number</label>
                            <input type="tel" id="phone" name="phone" placeholder="Enter your phone number" required>
                        </div>
                        <div class="form-group">
                            <label for="eventType">Event Type</label>
                            <select id="eventType" name="eventType" required>
                                <option value="">Select event type</option>
                                <option value="conference">Conference</option>
                                <option value="wedding">Wedding</option>
                                <option value="concert">Concert</option>
                                <option value="corporate">Corporate Event</option>
                                <option value="church">Church Service</option>
                                <option value="other">Other</option>
                            </select>
                        </div>
                        <div class="form-group">
                            <label for="eventDate">Event Date</label>
                            <input type="text" id="eventDate" name="eventDate" readonly required>
                        </div>
                        <div class="form-group">
                            <label for="package">Select Package</label>
                            <select id="package" name="package" required>
                                <option value="essential">Essential Stream - KES 30,000</option>
                                <option value="professional">Professional Broadcast - KES 70,000</option>
                                <option value="elite">Elite Production - KES 100,000</option>
                                <option value="custom">Custom Package</option>
                            </select>
                        </div>
                        <div class="form-group">
                            <label for="message">Additional Requirements</label>
                            <textarea id="message" name="message" rows="4" placeholder="Tell us about your event..."></textarea>
                        </div>
                        <button type="submit" class="cta-button" id="submitBooking">Submit Booking Request</button>
                    </form>
                </div>
            </div>
            
            <!-- Enhanced Crew Section -->
            <div class="crew-section">
                <h2 class="section-title animate">Our Expert Production Team</h2>
                <div class="crew-grid">
                    <div class="crew-item animate">
                        <div class="crew-icon"><i class="fas fa-user-tie"></i></div>
                        <div class="crew-name">Producer / Client Liaison</div>
                        <div class="crew-description">Your single point of contact managing show flow, client communication, and ensuring your vision is executed flawlessly.</div>
                    </div>
                    <div class="crew-item animate delay-1">
                        <div class="crew-icon"><i class="fas fa-video"></i></div>
                        <div class="crew-name">Technical Director</div>
                        <div class="crew-description">The maestro of the control room, executing live switching, cueing talent, and maintaining broadcast pacing.</div>
                    </div>
                    <div class="crew-item animate delay-2">
                        <div class="crew-icon"><i class="fas fa-camera"></i></div>
                        <div class="crew-name">Camera Operators</div>
                        <div class="crew-description">Skilled cinematographers capturing dynamic shots, following action, and maintaining visual storytelling.</div>
                    </div>
                    <div class="crew-item animate">
                        <div class="crew-icon"><i class="fas fa-microphone"></i></div>
                        <div class="crew-name">Sound Engineer</div>
                        <div class="crew-description">Audio specialist mixing multiple sources, eliminating noise, and ensuring crystal-clear audio quality.</div>
                    </div>
                    <div class="crew-item animate delay-1">
                        <div class="crew-icon"><i class="fas fa-broadcast-tower"></i></div>
                        <div class="crew-name">Stream Engineer</div>
                        <div class="crew-description">Technical expert managing multi-platform distribution, monitoring stream health, and ensuring broadcast stability.</div>
                    </div>
                    <div class="crew-item animate delay-2">
                        <div class="crew-icon"><i class="fas fa-paint-brush"></i></div>
                        <div class="crew-name">Graphics Operator</div>
                        <div class="crew-description">Creative professional managing live graphics, lower thirds, and ensuring brand consistency throughout the broadcast.</div>
                    </div>
                </div>
            </div>
            
            <!-- Enhanced Testimonials -->
            <div class="testimonials">
                <h2 class="section-title animate">What Our Clients Say</h2>
                <div class="testimonial-grid">
                    <div class="testimonial animate">
                        <div class="testimonial-text">
                            "WUEEH TV transformed our annual conference into a cinematic experience. The 3-camera setup and professional graphics made our virtual attendees feel like they were in the front row. The team's expertise was evident in every aspect of the production."
                        </div>
                        <div class="testimonial-author">
                            <div class="author-avatar">JK</div>
                            <div class="author-info">
                                <h4>James Kariuki</h4>
                                <p>Event Director, TechConnect Africa</p>
                            </div>
                        </div>
                    </div>
                    <div class="testimonial animate delay-1">
                        <div class="testimonial-text">
                            "We've used WUEEH TV for three consecutive university graduation ceremonies. Their professionalism, attention to detail, and ability to handle last-minute changes make them our go-to production partner. The highlight reels they create are absolutely stunning."
                        </div>
                        <div class="testimonial-author">
                            <div class="author-avatar">NM</div>
                            <div class="author-info">
                                <h4>Naomi Mwangi</h4>
                                <p>Events Manager, University of Nairobi</p>
                            </div>
                        </div>
                    </div>
                    <div class="testimonial animate delay-2">
                        <div class="testimonial-text">
                            "The social interaction features took our product launch to another level. Seeing audience comments and questions displayed in real-time created an engaging experience that traditional streaming can't match. Worth every shilling!"
                        </div>
                        <div class="testimonial-author">
                            <div class="author-avatar">DW</div>
                            <div class="author-info">
                                <h4>David Waweru</h4>
                                <p>Marketing Director, Innovate Kenya</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            
            <!-- Enhanced Terms Section -->
            <div class="terms">
                <h2 class="section-title animate">Booking & Terms</h2>
                <div class="terms-grid">
                    <div class="term-item animate">
                        <h3><i class="fas fa-hand-holding-usd"></i> Payment Structure</h3>
                        <p>40% deposit required to secure your booking (non-refundable if cancellation occurs within 7 days of event). Remaining balance due upon delivery of final files or within 7 business days post-event.</p>
                    </div>
                    <div class="term-item animate delay-1">
                        <h3><i class="fas fa-calendar-times"></i> Cancellation Policy</h3>
                        <p>Cancellation more than 14 days before event: full deposit refund. Cancellation 7-14 days before: 50% refund of deposit. Cancellation less than 7 days before: no deposit refund. Rescheduling subject to availability.</p>
                    </div>
                    <div class="term-item animate delay-2">
                        <h3><i class="fas fa-shipping-fast"></i> Delivery Timeline</h3>
                        <p>Standard delivery: first edit within 5-7 business days. Rush delivery (48-72 hours) available at 30-50% surcharge. All project files retained for 30 days unless purchased separately.</p>
                    </div>
                    <div class="term-item animate">
                        <h3><i class="fas fa-map-marker-alt"></i> Travel & Logistics</h3>
                        <p>All prices include services within Nairobi. Events outside Nairobi may incur additional travel and accommodation fees. Venue site survey recommended for optimal technical setup.</p>
                    </div>
                </div>
            </div>
            
            <!-- Enhanced Footer -->
            <div class="footer" id="contact">
                <h3 class="animate">Ready to Elevate Your Event?</h3>
                <p class="animate delay-1">Contact us today to discuss your production needs and receive a customized quote.</p>
                <div class="contact-info">
                    <div class="contact-item animate">
                        <i class="fas fa-phone"></i>
                        <span>+254 711 468301</span>
                    </div>
                    <div class="contact-item animate delay-1">
                        <i class="fas fa-envelope"></i>
                        <span>wueehtv@gmail.com</span>
                    </div>
                    <div class="contact-item animate delay-2">
                        <i class="fas fa-globe"></i>
                        <span>www.wueehtv.com</span>
                    </div>
                </div>
                <div class="social-links">
                    <a href="#" class="social-link animate"><i class="fab fa-facebook-f"></i></a>
                    <a href="#" class="social-link animate delay-1"><i class="fab fa-twitter"></i></a>
                    <a href="#" class="social-link animate delay-2"><i class="fab fa-instagram"></i></a>
                    <a href="#" class="social-link animate"><i class="fab fa-youtube"></i></a>
                    <a href="#" class="social-link animate delay-1"><i class="fab fa-linkedin-in"></i></a>
                </div>
            </div>
        </div>
    </div>

    <!-- AI Chatbot -->
    <div class="chat-bubble" id="chatBubble">
        <i class="fas fa-comments"></i>
    </div>

    <div class="chat-window" id="chatWindow">
        <div class="chat-header">
            <div class="chat-avatar">WT</div>
            <div class="chat-info">
                <h4>WUEEH TV AI Assistant</h4>
                <p>We're here to help!</p>
            </div>
            <div class="chat-close" id="chatClose">
                <i class="fas fa-times"></i>
            </div>
        </div>
        <div class="chat-body" id="chatBody">
            <div class="chat-message bot-message">
                Hello! I'm your WUEEH TV AI assistant. I can help you with package information, booking details, technical questions, or anything else about our streaming services. How can I assist you today?
            </div>
        </div>
        <div class="chat-input">
            <input type="text" id="chatInput" placeholder="Type your message...">
            <div class="chat-send" id="chatSend">
                <i class="fas fa-paper-plane"></i>
            </div>
        </div>
    </div>

    <!-- Mobile Navigation -->
    <div class="mobile-nav">
        <a href="#booking">Book Now</a>
    </div>

    <script>
        // Comprehensive JavaScript implementing all features
        document.addEventListener('DOMContentLoaded', function() {
            // 1. Progress Bar
            window.addEventListener('scroll', function() {
                const winHeight = window.innerHeight;
                const docHeight = document.documentElement.scrollHeight;
                const scrollTop = window.pageYOffset;
                const scrollPercent = (scrollTop / (docHeight - winHeight)) * 100;
                document.getElementById('progressBar').style.width = scrollPercent + '%';
            });

            // 2. Theme Toggle
            const themeToggle = document.getElementById('themeToggle');
            themeToggle.addEventListener('click', function() {
                const currentTheme = document.body.getAttribute('data-theme');
                if (currentTheme === 'dark') {
                    document.body.removeAttribute('data-theme');
                    themeToggle.innerHTML = '<i class="fas fa-moon"></i>';
                    localStorage.setItem('theme', 'light');
                } else {
                    document.body.setAttribute('data-theme', 'dark');
                    themeToggle.innerHTML = '<i class="fas fa-sun"></i>';
                    localStorage.setItem('theme', 'dark');
                }
            });

            // Load saved theme
            const savedTheme = localStorage.getItem('theme');
            if (savedTheme === 'dark') {
                document.body.setAttribute('data-theme', 'dark');
                themeToggle.innerHTML = '<i class="fas fa-sun"></i>';
            }

            // 3. Voice Control
            const voiceControl = document.getElementById('voiceControl');
            const voiceFeedback = document.getElementById('voiceFeedback');
            let recognition;

            if ('webkitSpeechRecognition' in window) {
                recognition = new webkitSpeechRecognition();
                recognition.continuous = false;
                recognition.interimResults = false;
                recognition.lang = 'en-US';

                recognition.onstart = function() {
                    voiceControl.classList.add('listening');
                    voiceFeedback.textContent = "Listening...";
                    voiceFeedback.style.display = 'block';
                };

                recognition.onresult = function(event) {
                    const transcript = event.results[0][0].transcript.toLowerCase();
                    voiceFeedback.textContent = "You said: " + transcript;
                    
                    // Process voice commands
                    if (transcript.includes('book') || transcript.includes('booking')) {
                        document.getElementById('booking').scrollIntoView({ behavior: 'smooth' });
                    } else if (transcript.includes('package') || transcript.includes('pricing')) {
                        document.getElementById('customizer').scrollIntoView({ behavior: 'smooth' });
                    } else if (transcript.includes('contact')) {
                        document.getElementById('contact').scrollIntoView({ behavior: 'smooth' });
                    } else if (transcript.includes('demo') || transcript.includes('live')) {
                        document.getElementById('liveDemo').scrollIntoView({ behavior: 'smooth' });
                    } else {
                        voiceFeedback.textContent = "Command not recognized. Try: 'book', 'package', 'contact', or 'demo'";
                    }
                    
                    setTimeout(() => {
                        voiceFeedback.style.display = 'none';
                    }, 3000);
                };

                recognition.onerror = function() {
                    voiceControl.classList.remove('listening');
                    voiceFeedback.textContent = "Error occurred. Please try again.";
                    setTimeout(() => {
                        voiceFeedback.style.display = 'none';
                    }, 3000);
                };

                recognition.onend = function() {
                    voiceControl.classList.remove('listening');
                };
            }

            voiceControl.addEventListener('click', function() {
                if (recognition) {
                    recognition.start();
                } else {
                    voiceFeedback.textContent = "Speech recognition not supported in your browser";
                    voiceFeedback.style.display = 'block';
                    setTimeout(() => {
                        voiceFeedback.style.display = 'none';
                    }, 3000);
                }
            });

            // 4. Language Switcher
            const langBtn = document.getElementById('langBtn');
            const langDropdown = document.getElementById('langDropdown');
            const langOptions = document.querySelectorAll('.lang-option');

            langBtn.addEventListener('click', function() {
                langDropdown.style.display = langDropdown.style.display === 'block' ? 'none' : 'block';
            });

            langOptions.forEach(option => {
                option.addEventListener('click', function() {
                    const lang = this.getAttribute('data-lang');
                    langBtn.querySelector('span').textContent = lang.toUpperCase();
                    langDropdown.style.display = 'none';
                    
                    // In a real implementation, you would load translations here
                    alert('Language changed to ' + this.textContent);
                });
            });

            // 5. Floating Elements
            const floatingContainer = document.getElementById('floatingElements');
            for (let i = 0; i < 15; i++) {
                const element = document.createElement('div');
                element.classList.add('floating-element');
                element.style.width = Math.random() * 30 + 10 + 'px';
                element.style.height = element.style.width;
                element.style.left = Math.random() * 100 + '%';
                element.style.animationDelay = Math.random() * 15 + 's';
                element.style.animationDuration = Math.random() * 10 + 15 + 's';
                floatingContainer.appendChild(element);
            }

            // 6. Live Stream Demo
            const playDemo = document.getElementById('playDemo');
            const fullscreen = document.getElementById('fullscreen');
            const vrMode = document.getElementById('vrMode');
            const streamPlaceholder = document.querySelector('.stream-placeholder');

            playDemo.addEventListener('click', function() {
                const iframe = streamPlaceholder.querySelector('iframe');
                iframe.src = iframe.src.replace('autoplay=0', 'autoplay=1');
            });

            fullscreen.addEventListener('click', function() {
                const iframe = streamPlaceholder.querySelector('iframe');
                if (iframe.requestFullscreen) {
                    iframe.requestFullscreen();
                } else if (iframe.webkitRequestFullscreen) {
                    iframe.webkitRequestFullscreen();
                } else if (iframe.msRequestFullscreen) {
                    iframe.msRequestFullscreen();
                }
            });

            vrMode.addEventListener('click', function() {
                alert('VR mode would launch here in a full implementation');
            });

            // 7. Package Customizer
            const featureToggles = document.querySelectorAll('.feature-toggle');
            const totalPrice = document.querySelector('.total-price');
            let basePrice = 70000;

            featureToggles.forEach(toggle => {
                toggle.addEventListener('click', function() {
                    const category = this.closest('.feature-category');
                    const categoryToggles = category.querySelectorAll('.feature-toggle');
                    
                    // Deactivate all toggles in the same category
                    categoryToggles.forEach(t => t.classList.remove('active'));
                    
                    // Activate the clicked toggle
                    this.classList.add('active');
                    
                    // Update price
                    updateTotalPrice();
                });
            });

            function updateTotalPrice() {
                let newPrice = basePrice;
                featureToggles.forEach(toggle => {
                    if (toggle.classList.contains('active')) {
                        const price = parseInt(toggle.getAttribute('data-price'));
                        newPrice += price;
                    }
                });
                
                totalPrice.textContent = 'KES ' + newPrice.toLocaleString();
            }

            // 8. Portfolio Lightbox
            const portfolioItems = document.querySelectorAll('.portfolio-item');
            const lightbox = document.getElementById('lightbox');
            const lightboxImage = document.getElementById('lightboxImage');
            const lightboxClose = document.getElementById('lightboxClose');

            portfolioItems.forEach(item => {
                item.addEventListener('click', function() {
                    const imgSrc = this.querySelector('img').src;
                    lightboxImage.src = imgSrc;
                    lightbox.style.display = 'flex';
                });
            });

            lightboxClose.addEventListener('click', function() {
                lightbox.style.display = 'none';
            });

            lightbox.addEventListener('click', function(e) {
                if (e.target === lightbox) {
                    lightbox.style.display = 'none';
                }
            });

            // 9. Booking Calendar
            const calendarGrid = document.getElementById('calendarGrid');
            const currentMonth = document.getElementById('currentMonth');
            const prevMonth = document.getElementById('prevMonth');
            const nextMonth = document.getElementById('nextMonth');
            const eventDate = document.getElementById('eventDate');

            let currentDate = new Date();
            let selectedDate = null;

            function renderCalendar() {
                calendarGrid.innerHTML = '';
                
                const year = currentDate.getFullYear();
                const month = currentDate.getMonth();
                
                currentMonth.textContent = currentDate.toLocaleString('default', { month: 'long', year: 'numeric' });
                
                const firstDay = new Date(year, month, 1);
                const lastDay = new Date(year, month + 1, 0);
                const daysInMonth = lastDay.getDate();
                
                // Add day headers
                const days = ['Sun', 'Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat'];
                days.forEach(day => {
                    const dayElement = document.createElement('div');
                    dayElement.textContent = day;
                    dayElement.style.fontWeight = 'bold';
                    calendarGrid.appendChild(dayElement);
                });
                
                // Add empty cells for days before the first day of the month
                for (let i = 0; i < firstDay.getDay(); i++) {
                    const emptyElement = document.createElement('div');
                    calendarGrid.appendChild(emptyElement);
                }
                
                // Add days of the month
                for (let day = 1; day <= daysInMonth; day++) {
                    const dayElement = document.createElement('div');
                    dayElement.textContent = day;
                    dayElement.classList.add('calendar-day');
                    
                    // Randomly mark some days as booked for demo
                    if (Math.random() > 0.7) {
                        dayElement.classList.add('booked');
                    } else {
                        dayElement.classList.add('available');
                    }
                    
                    dayElement.addEventListener('click', function() {
                        if (!this.classList.contains('booked')) {
                            // Remove selection from other days
                            document.querySelectorAll('.calendar-day').forEach(d => {
                                d.style.background = '';
                                d.style.color = '';
                            });
                            
                            // Select this day
                            this.style.background = 'var(--accent)';
                            this.style.color = 'white';
                            
                            selectedDate = new Date(year, month, day);
                            eventDate.value = selectedDate.toLocaleDateString();
                        }
                    });
                    
                    calendarGrid.appendChild(dayElement);
                }
            }

            prevMonth.addEventListener('click', function() {
                currentDate.setMonth(currentDate.getMonth() - 1);
                renderCalendar();
            });

            nextMonth.addEventListener('click', function() {
                currentDate.setMonth(currentDate.getMonth() + 1);
                renderCalendar();
            });

            renderCalendar();

            // 10. AI Chatbot
            const chatBubble = document.getElementById('chatBubble');
            const chatWindow = document.getElementById('chatWindow');
            const chatClose = document.getElementById('chatClose');
            const chatSend = document.getElementById('chatSend');
            const chatInput = document.getElementById('chatInput');
            const chatBody = document.getElementById('chatBody');
            
            let chatOpen = false;
            
            chatBubble.addEventListener('click', function() {
                if (!chatOpen) {
                    chatWindow.style.display = 'flex';
                    chatOpen = true;
                    setTimeout(() => {
                        chatBody.scrollTop = chatBody.scrollHeight;
                    }, 100);
                }
            });
            
            chatClose.addEventListener('click', function() {
                chatWindow.style.display = 'none';
                chatOpen = false;
            });
            
            function addMessage(text, isUser) {
                const messageDiv = document.createElement('div');
                messageDiv.classList.add('chat-message');
                messageDiv.classList.add(isUser ? 'user-message' : 'bot-message');
                messageDiv.textContent = text;
                chatBody.appendChild(messageDiv);
                chatBody.scrollTop = chatBody.scrollHeight;
            }
            
            function getAIResponse(userMessage) {
                const responses = {
                    'hello': "Hello! How can I help you with your streaming needs today?",
                    'hi': "Hi there! Ready to create an amazing streaming experience?",
                    'price': "Our packages start from KES 30,000. Would you like details about our Essential, Professional, or Elite packages?",
                    'booking': "I can help you book an event! Please visit our booking section or tell me your event date and type.",
                    'package': "We offer three main packages: Essential (KES 30K), Professional (KES 70K), and Elite (KES 100K). Which one interests you?",
                    'contact': "You can reach us at +254 711 468301 or wueehtv@gmail.com. Our team is available 24/7 for consultations.",
                    'default': "I'm here to help with package information, booking, technical questions, or anything else about WUEEH TV services. What would you like to know?"
                };
                
                const lowerMessage = userMessage.toLowerCase();
                
                if (lowerMessage.includes('hello') || lowerMessage.includes('hi')) {
                    return responses.hello;
                } else if (lowerMessage.includes('price') || lowerMessage.includes('cost')) {
                    return responses.price;
                } else if (lowerMessage.includes('book') || lowerMessage.includes('schedule')) {
                    return responses.booking;
                } else if (lowerMessage.includes('package') || lowerMessage.includes('plan')) {
                    return responses.package;
                } else if (lowerMessage.includes('contact') || lowerMessage.includes('call')) {
                    return responses.contact;
                } else {
                    return responses.default;
                }
            }
            
            chatSend.addEventListener('click', function() {
                const message = chatInput.value.trim();
                if (message) {
                    addMessage(message, true);
                    chatInput.value = '';
                    
                    // Simulate AI thinking
                    setTimeout(() => {
                        addMessage(getAIResponse(message), false);
                    }, 1000);
                }
            });
            
            chatInput.addEventListener('keypress', function(e) {
                if (e.key === 'Enter') {
                    chatSend.click();
                }
            });

            // 11. Enhanced Animations
            const animatedElements = document.querySelectorAll('.animate');
            
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.style.opacity = '1';
                        entry.target.style.transform = 'translateY(0)';
                    }
                });
            }, { threshold: 0.1 });
            
            animatedElements.forEach(element => {
                element.style.opacity = '0';
                element.style.transform = 'translateY(40px)';
                element.style.transition = 'opacity 0.8s ease, transform 0.8s ease';
                observer.observe(element);
            });
            
            // 12. Smooth Scrolling
            document.querySelectorAll('a[href^="#"]').forEach(anchor => {
                anchor.addEventListener('click', function (e) {
                    e.preventDefault();
                    const target = document.querySelector(this.getAttribute('href'));
                    if (target) {
                        target.scrollIntoView({
                            behavior: 'smooth',
                            block: 'start'
                        });
                    }
                });
            });

            // 13. Package Card Hover Effects
            const packageCards = document.querySelectorAll('.package-card');
            
            packageCards.forEach(card => {
                card.addEventListener('mouseenter', function() {
                    this.style.transform = this.classList.contains('featured') 
                        ? 'scale(1.05) translateY(-20px)' 
                        : 'translateY(-20px)';
                });
                
                card.addEventListener('mouseleave', function() {
                    this.style.transform = this.classList.contains('featured') 
                        ? 'scale(1.05)' 
                        : 'translateY(0)';
                });
            });
        });
    </script>
</body>
</html>
